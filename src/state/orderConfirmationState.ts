// Returns the initial closed state for the order confirmation modal.
export function resolveInitialOrderConfirmationOpenState(): boolean {
  return false;
}

// Returns an empty typed collection for initial order-related lists.
export function resolveInitialOrderCollection<T>(): T[] {
  return [];
}
